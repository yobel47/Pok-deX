import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

function Icon() {
  return (
    <View>
      <Text>Icon</Text>
    </View>
  );
}

export default Icon;

const styles = StyleSheet.create({});
